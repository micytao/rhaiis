apiVersion: v1
kind: Service
metadata:
  name: rhaiis-service
  namespace: rhaiis
  labels:
    app: rhaiis
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: rhaiis
